# Add public headers files to installation
set( STARSH_PUBLIC_HDR 
    starsh.h
    starsh-spatial.h
    starsh-rndtiled.h )

#set_target_properties( starsh PROPERTIES PUBLIC_HEADER "${STARSH_PUB_HDR}" )

#install(TARGETS starsh PUBLIC_HEADER DESTINATION include/starsh )
