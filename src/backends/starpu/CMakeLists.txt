set(SRC)
add_subdirectory("blrm")
add_subdirectory("kernels")
add_library(backends_starpu OBJECT ${SRC})
target_include_directories(backends_starpu PRIVATE "${CMAKE_SOURCE_DIR}/include")
target_compile_options(backends_starpu PRIVATE "-std=gnu99")
